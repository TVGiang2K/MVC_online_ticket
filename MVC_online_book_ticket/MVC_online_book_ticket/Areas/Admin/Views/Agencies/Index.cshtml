@model IEnumerable<MVC_online_book_ticket.Models.Agencies>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AgenciesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AgenciesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AgenciesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
